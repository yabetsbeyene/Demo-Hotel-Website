import { AdminShell } from '@/components/admin-shell';
import { Icon } from '@/components/icon';
import { roomInventory } from '@/lib/demo-data';

export default function RoomsPage() { return <AdminShell><div className="page-wrap"><div className="page-heading"><div><p className="eyebrow">Inventory</p><h1>Room types<span>.</span></h1><p className="lede">Keep rates, capacity, and availability accurate for every room category.</p></div><button className="primary-button"><Icon name="plus" size={17} /> Add room type</button></div><div className="room-grid">{roomInventory.map((room) => <section className="room-card" key={room.name}><div className="room-photo"><Icon name="bed" size={28} /><span>{room.total} rooms total</span></div><div className="room-card-body"><div><h2>{room.name}</h2><p>{room.rate} / night</p></div><button className="row-more" aria-label={`Actions for ${room.name}`}><Icon name="more" /></button><div className="room-card-meta"><span><b>{room.total - room.occupied}</b> available</span><span><b>{room.occupied}</b> occupied</span></div><div className="inventory-bar"><div style={{ width: `${Math.round((room.occupied / room.total) * 100)}%` }} /></div></div></section>)}</div><p className="demo-note"><span>Demo data</span> — rates and availability will be sourced from <code>room_types</code> and <code>bookings</code>.</p></div></AdminShell> }
